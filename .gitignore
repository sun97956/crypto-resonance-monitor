# 依赖 / 本地文件
.venv/
venv/
__pycache__/
*.pyc

# 私密配置（含真实 chat_id / token）
config.json
.env

# 运行时状态与日志（自动生成）
*.log
crypto_alert_state.json

# 回测数据（体积大，不进仓；可重新拉取）
user_data/
*.feather

# 系统文件
.DS_Store
Thumbs.db
